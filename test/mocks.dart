
// test/mocks.dart
import 'package:mockito/annotations.dart';
import 'package:myapp/services/networking_service.dart';

// Add the following annotation to generate a mock class for PacketService.
@GenerateMocks([PacketService])
void main() {
  // This file is used to generate the mock classes.
  // It does not need to contain any code in the main function.
}
